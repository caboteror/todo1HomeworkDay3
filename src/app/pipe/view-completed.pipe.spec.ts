import { ViewCompletedPipe } from './view-completed.pipe';

describe('ViewCompletedPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewCompletedPipe();
    expect(pipe).toBeTruthy();
  });
});
