import { NewTaskDirective } from './new-task.directive';

describe('NewTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new NewTaskDirective();
    expect(directive).toBeTruthy();
  });
});
